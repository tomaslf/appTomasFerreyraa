class Review {
    constructor (id, title, image, cityName){
        this.id= id.toString(),
        this.title= title,
        this.image= image,
        this.cityName= cityName
    }
}

export default Review